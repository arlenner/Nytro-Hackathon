import { Html } from 'olive-spa'
import { ACTIONS } from '../../env'

export const fxConnectWallet = (model, action) => {
    if(action[0] !== ACTIONS.TRY_CONNECT_WALLET)
    const hx = Html.from(document.querySelector('.wallet-input'))
    
    return action
}